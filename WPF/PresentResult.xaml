<Window x:Class="AttentionOfUser.PresentResult"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AttentionOfUser"
        mc:Ignorable="d"
        Title="PresentResult" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Image x:Name="iOri" x:FieldModifier="public" Grid.Column="0"/>
        <Image x:Name="iActivated" x:FieldModifier="public" Grid.Column="1"/>
        <Image x:Name="iActivation" x:FieldModifier="public" Grid.Column="2"/>
        <Button Grid.Row="1" Click="OK" >Accept</Button>
        <Button Grid.Row="1" Grid.Column="2" Click="REFUSE">Refuse</Button>
    </Grid>
</Window>
